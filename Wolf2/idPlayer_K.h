#pragma once


/// <summary>
/// this is an attempt to solve our issue with generating classes which seesm to be flawed whatever way we look at it either from specific var too painful to find, or padding/alignment issues. This should only get use the variables we need and to update it if the game did update, we would check the lib file we still generate and check which offsets have changed.
/// </summary>

class idPlayer_K
{
private:

public:


};

